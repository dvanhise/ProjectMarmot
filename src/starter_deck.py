from utils.card_registry import get_new_card


def add_starter_cards(player):
    player.add_card(get_new_card('spike'))
    player.add_card(get_new_card('spike'))
    player.add_card(get_new_card('spike'))
    player.add_card(get_new_card('spike'))
    player.add_card(get_new_card('amplifier'))
    player.add_card(get_new_card('amplifier'))
    player.add_card(get_new_card('security-group'))
    player.add_card(get_new_card('security-group'))